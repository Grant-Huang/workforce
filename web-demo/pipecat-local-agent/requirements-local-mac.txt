# Mac Mini ML stack — install on Apple Silicon with MPS support
-r requirements.txt

torch>=2.2.0
funasr>=1.1.0
lancedb>=0.6.0
sentence-transformers>=2.7.0

# Qwen3-TTS: install your local package / fork here, then implement
# local_services/tts_runtime.py or set LOCAL_TTS_MODULE
