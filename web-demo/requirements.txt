aiohttp>=3.9
websockets>=12.0
python-dotenv>=1.0
# LanceDB：长期记忆（profile / semantic / source_index）持久化与近邻检索。
# Phase 1 用简易 hash embedding，不引入重型向量模型。
lancedb>=0.17.0
pyarrow>=14.0.0
pandas>=2.0.0
# Tavily Search API：WebSearchProvider 主路径（需 TAVILY_API_KEY）。
# 未配置或失败时回退 Google News RSS。不再依赖 ddgs/DuckDuckGo HTML 抓取。
# tavily-python 可选；当前用标准库 HTTPS 调用官方 REST，零额外依赖。
# （保留说明：若改用官方 SDK，可 pip install tavily-python）
